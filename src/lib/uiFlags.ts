// Paid re-generation UI (restyle chips + "Clear the room & redesign").
// Each use is a billed image generation (₹12–15; clear-room is two), so it's
// hidden during the free-design feedback phase. Flip to true to re-enable —
// the API routes stay live either way.
export const RESTYLE_UI_ENABLED = false;
